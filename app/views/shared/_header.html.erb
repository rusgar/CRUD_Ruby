<%= link_to products_path do %>
   <h1><%= t('app_name') %></h1>
<% end %>
<%
=begin%>
 <%= Current.user.inspect %>> 
<%
=end%>
<div><%= button_to link_to t('.login'), new_session_path unless Current.user %><br>
<%= button_to link_to t('.register'), new_user_path unless Current.user %></div><br>
<%= button_to link_to t('.add'), new_product_path if Current.user  %><br>
<%= button_to link_to t('.categories') , categories_path if Current.user&.admin? %><br>
<%= button_to link_to t('.logout'), session_path(Current.user.id),
    data:{ 
         turbo_method: :delete,
         turbo_confirm: t('common.confirm')
       } if Current.user %></div><br>

 